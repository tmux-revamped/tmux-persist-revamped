src/persist.sh 600 the option inventory, the tmux seams and the save path share
the format version and the redaction table, so splitting them further exports
state that has no meaning outside this file. The restore path, the reporting
commands and the login agent already live in their own files.
